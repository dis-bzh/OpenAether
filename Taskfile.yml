version: "3"

tasks:
  default:
    desc: Show available tasks
    cmds:
      - task --list

  setup:
    desc: Install dependencies (OpenTofu, Talosctl, Kubectl, Task, pre-commit)
    cmds:
      - ./scripts/setup.sh

  lint:
    desc: Lint and format code (OpenTofu, YAML)
    cmds:
      - tofu fmt -check -recursive infrastructure/opentofu
      - yamllint -c infrastructure/.yamllint apps/ infrastructure/
      - tflint --recursive infrastructure/opentofu/cluster

  fmt:
    desc: Format OpenTofu code
    cmds:
      - tofu fmt -recursive infrastructure/opentofu

  validate:
    desc: Validate the cluster OpenTofu root
    dir: infrastructure/opentofu/cluster
    cmds:
      - tofu init -backend=false
      - tofu validate

  security:
    desc: Run security scans (Trivy, Checkov, Gitleaks)
    cmds:
      - trivy config infrastructure/
      - checkov -d infrastructure/opentofu/cluster
      - gitleaks detect --source . --no-git

  test:
    desc: Run OpenTofu tests (cluster root)
    dir: infrastructure/opentofu/cluster
    cmds:
      - tofu test

  # ===========================================================================
  # Talos image (decoupled — build once per Talos version, reused by clusters)
  # ===========================================================================

  talos-image:
    desc: "Build + publish the Talos image for a provider. Usage: task talos-image PROVIDER=scaleway|ovh|outscale [VERSION=v1.13.3]"
    vars:
      PROVIDER: '{{.PROVIDER | default "scaleway"}}'
      VERSION: '{{.VERSION | default "v1.13.3"}}'
    cmds:
      - ./scripts/talos-image.sh {{.PROVIDER}} {{.VERSION}}

  # ===========================================================================
  # Cluster Lifecycle
  # ===========================================================================

  infra-management:
    desc: "Deploy the management cluster. Usage: task infra-management [PROVIDER=scaleway|ovh|outscale]"
    dir: infrastructure/opentofu/cluster
    vars:
      PROVIDER: '{{.PROVIDER | default "scaleway"}}'
    env:
      AWS_ACCESS_KEY_ID:
        sh: ../../../scripts/resolve-s3-cred.sh {{.PROVIDER}} ak
      AWS_SECRET_ACCESS_KEY:
        sh: ../../../scripts/resolve-s3-cred.sh {{.PROVIDER}} sk
      TF_VAR_outscale_access_key_id:
        sh: '[ "{{.PROVIDER}}" = outscale ] && ../../../scripts/resolve-s3-cred.sh outscale ak || true'
      TF_VAR_outscale_secret_key_id:
        sh: '[ "{{.PROVIDER}}" = outscale ] && ../../../scripts/resolve-s3-cred.sh outscale sk || true'
    cmds:
      - ../../../scripts/ensure-buckets.sh envs/management-{{.PROVIDER}}.tfvars
      - tofu init -reconfigure $(../../../scripts/tf-backend.sh envs/management-{{.PROVIDER}}.tfvars)
      - tofu apply -var-file="envs/management-{{.PROVIDER}}.tfvars" -var talos_bootstrap=false
      - ../../../scripts/backup-state.sh . || echo "⚠ state backup skipped/failed (does the -backup bucket exist?)"

  infra-workload:
    desc: "Deploy a workload cluster. Usage: task infra-workload PROVIDER=ovh"
    dir: infrastructure/opentofu/cluster
    vars:
      PROVIDER: '{{.PROVIDER | default "scaleway"}}'
    env:
      AWS_ACCESS_KEY_ID:
        sh: ../../../scripts/resolve-s3-cred.sh {{.PROVIDER}} ak
      AWS_SECRET_ACCESS_KEY:
        sh: ../../../scripts/resolve-s3-cred.sh {{.PROVIDER}} sk
      TF_VAR_outscale_access_key_id:
        sh: '[ "{{.PROVIDER}}" = outscale ] && ../../../scripts/resolve-s3-cred.sh outscale ak || true'
      TF_VAR_outscale_secret_key_id:
        sh: '[ "{{.PROVIDER}}" = outscale ] && ../../../scripts/resolve-s3-cred.sh outscale sk || true'
    cmds:
      - ../../../scripts/ensure-buckets.sh envs/workload-{{.PROVIDER}}.tfvars
      - tofu init -reconfigure $(../../../scripts/tf-backend.sh envs/workload-{{.PROVIDER}}.tfvars)
      - tofu apply -var-file="envs/workload-{{.PROVIDER}}.tfvars" -var talos_bootstrap=false
      - ../../../scripts/backup-state.sh . || echo "⚠ state backup skipped/failed (does the -backup bucket exist?)"

  management:
    desc: "Phase 2: open SSH tunnels (from state) + bootstrap the management cluster. Usage: task management [PROVIDER=scaleway|ovh|outscale] KEY=~/.ssh/yourkey"
    dir: infrastructure/opentofu/cluster
    vars:
      PROVIDER: '{{.PROVIDER | default "scaleway"}}'
      KEY: '{{.KEY | default "~/.ssh/id_ed25519"}}'
    env:
      AWS_ACCESS_KEY_ID:
        sh: ../../../scripts/resolve-s3-cred.sh {{.PROVIDER}} ak
      AWS_SECRET_ACCESS_KEY:
        sh: ../../../scripts/resolve-s3-cred.sh {{.PROVIDER}} sk
      TF_VAR_outscale_access_key_id:
        sh: '[ "{{.PROVIDER}}" = outscale ] && ../../../scripts/resolve-s3-cred.sh outscale ak || true'
      TF_VAR_outscale_secret_key_id:
        sh: '[ "{{.PROVIDER}}" = outscale ] && ../../../scripts/resolve-s3-cred.sh outscale sk || true'
    cmds:
      - tofu init -reconfigure $(../../../scripts/tf-backend.sh envs/management-{{.PROVIDER}}.tfvars)
      - SSH_KEY="{{.KEY}}" ../../../scripts/talos-tunnels.sh open .
      - tofu apply -var-file="envs/management-{{.PROVIDER}}.tfvars" -var talos_bootstrap=true
      - ../../../scripts/backup-state.sh . || echo "⚠ state backup skipped/failed (does the -backup bucket exist?)"

  workload:
    desc: "Phase 2: open tunnels + bootstrap a workload cluster. Usage: task workload PROVIDER=ovh KEY=~/.ssh/yourkey"
    dir: infrastructure/opentofu/cluster
    vars:
      PROVIDER: '{{.PROVIDER | default "scaleway"}}'
      KEY: '{{.KEY | default "~/.ssh/id_ed25519"}}'
    env:
      AWS_ACCESS_KEY_ID:
        sh: ../../../scripts/resolve-s3-cred.sh {{.PROVIDER}} ak
      AWS_SECRET_ACCESS_KEY:
        sh: ../../../scripts/resolve-s3-cred.sh {{.PROVIDER}} sk
      TF_VAR_outscale_access_key_id:
        sh: '[ "{{.PROVIDER}}" = outscale ] && ../../../scripts/resolve-s3-cred.sh outscale ak || true'
      TF_VAR_outscale_secret_key_id:
        sh: '[ "{{.PROVIDER}}" = outscale ] && ../../../scripts/resolve-s3-cred.sh outscale sk || true'
    cmds:
      - tofu init -reconfigure $(../../../scripts/tf-backend.sh envs/workload-{{.PROVIDER}}.tfvars)
      - SSH_KEY="{{.KEY}}" ../../../scripts/talos-tunnels.sh open .
      - tofu apply -var-file="envs/workload-{{.PROVIDER}}.tfvars" -var talos_bootstrap=true
      - ../../../scripts/backup-state.sh . || echo "⚠ state backup skipped/failed (does the -backup bucket exist?)"

  close-tunnels:
    desc: "Close the Talos API SSH tunnels opened for Phase 2"
    cmds:
      - ./scripts/talos-tunnels.sh close infrastructure/opentofu/cluster

  backup-state:
    desc: "Replicate the (encrypted) tfstate to the -backup store (post-apply). Set BACKUP_AWS_* for a cross-provider replica."
    cmds:
      - ./scripts/backup-state.sh infrastructure/opentofu/cluster

  destroy-management:
    desc: "Destroy a management cluster. Usage: task destroy-management PROVIDER=ovh"
    dir: infrastructure/opentofu/cluster
    vars:
      PROVIDER: '{{.PROVIDER | default "scaleway"}}'
    env:
      AWS_ACCESS_KEY_ID:
        sh: ../../../scripts/resolve-s3-cred.sh {{.PROVIDER}} ak
      AWS_SECRET_ACCESS_KEY:
        sh: ../../../scripts/resolve-s3-cred.sh {{.PROVIDER}} sk
      TF_VAR_outscale_access_key_id:
        sh: '[ "{{.PROVIDER}}" = outscale ] && ../../../scripts/resolve-s3-cred.sh outscale ak || true'
      TF_VAR_outscale_secret_key_id:
        sh: '[ "{{.PROVIDER}}" = outscale ] && ../../../scripts/resolve-s3-cred.sh outscale sk || true'
    cmds:
      - tofu init -reconfigure $(../../../scripts/tf-backend.sh envs/management-{{.PROVIDER}}.tfvars)
      - tofu state rm module.talos.talos_machine_secrets.this
      - tofu destroy -var-file="envs/management-{{.PROVIDER}}.tfvars"

  destroy-workload:
    desc: "Destroy a workload cluster. Usage: task destroy-workload PROVIDER=ovh"
    dir: infrastructure/opentofu/cluster
    vars:
      PROVIDER: '{{.PROVIDER | default "scaleway"}}'
    env:
      AWS_ACCESS_KEY_ID:
        sh: ../../../scripts/resolve-s3-cred.sh {{.PROVIDER}} ak
      AWS_SECRET_ACCESS_KEY:
        sh: ../../../scripts/resolve-s3-cred.sh {{.PROVIDER}} sk
      TF_VAR_outscale_access_key_id:
        sh: '[ "{{.PROVIDER}}" = outscale ] && ../../../scripts/resolve-s3-cred.sh outscale ak || true'
      TF_VAR_outscale_secret_key_id:
        sh: '[ "{{.PROVIDER}}" = outscale ] && ../../../scripts/resolve-s3-cred.sh outscale sk || true'
    cmds:
      - tofu init -reconfigure $(../../../scripts/tf-backend.sh envs/workload-{{.PROVIDER}}.tfvars)
      - tofu destroy -var-file="envs/workload-{{.PROVIDER}}.tfvars"

  # ===========================================================================
  # Multi-Cluster Management
  # ===========================================================================

  register-spoke:
    desc: "Register a spoke cluster in ArgoCD hub. Usage: task register-spoke CLUSTER=openaether-ovh-prod PROVIDER=ovh"
    vars:
      CLUSTER: '{{.CLUSTER | default ""}}'
      PROVIDER: '{{.PROVIDER | default ""}}'
    cmds:
      - ./scripts/register-spoke.sh "{{.CLUSTER}}" "{{.PROVIDER}}"

  failover:
    desc: "Cross-provider failover: stand up a 2nd management cluster on another cloud. Usage: task failover PROVIDER=ovh"
    vars:
      PROVIDER: '{{.PROVIDER | default "ovh"}}'
    cmds:
      - ./scripts/failover-management.sh "{{.PROVIDER}}"

  # ===========================================================================
  # Local Testing (Docker / WSL2)
  # ===========================================================================

  local-render-manifests:
    desc: "Render simplified Cilium manifest for local Docker testing (no WireGuard)"
    cmds:
      - ./scripts/render-bootstrap-manifests.sh --local

  local-up:
    desc: "Deploy the local 3-CP Talos cluster (config + containers + bootstrap + kubeconfig)"
    dir: infrastructure/opentofu-local
    cmds:
      - tofu init -upgrade > /dev/null 2>&1 || tofu init > /dev/null
      - |
        export TF_VAR_cilium_manifest="$(cat ../opentofu/cluster/bootstrap-manifests/cilium-local.yaml 2>/dev/null || echo 'placeholder')"
        tofu apply -var talos_bootstrap=true -auto-approve

  local-test:
    desc: "Full end-to-end local test: 3-CP cluster + etcd quorum + Cilium + ArgoCD + GitOps"
    cmds:
      - ./scripts/test-talos-local.sh

  local-status:
    desc: "Check local cluster health (3 CPs)"
    cmds:
      - |
        export KUBECONFIG=infrastructure/opentofu-local/kubeconfig
        export TALOSCONFIG=infrastructure/opentofu-local/talosconfig
        echo "=== etcd quorum ===" && talosctl --nodes 10.5.0.10 --endpoints 127.0.0.1:50000 etcd members || true
        echo "=== Kubernetes nodes ===" && kubectl get nodes -o wide || true
        echo "=== kube-system pods ===" && kubectl get pods -n kube-system || true
        echo "=== ArgoCD ===" && kubectl get applications -n management-gitops 2>/dev/null || echo "not deployed yet"

  local-argocd:
    desc: "Port-forward ArgoCD UI to localhost:8080"
    cmds:
      - |
        export KUBECONFIG=infrastructure/opentofu-local/kubeconfig
        echo "ArgoCD UI → https://localhost:8080"
        kubectl -n management-gitops port-forward svc/argocd-server 8080:443

  local-down:
    desc: "Destroy local Talos cluster and Docker containers"
    dir: infrastructure/opentofu-local
    cmds:
      - |
        export TF_VAR_cilium_manifest="placeholder"
        tofu destroy -auto-approve 2>/dev/null || true

  # ===========================================================================
  # Bootstrap & Secrets
  # ===========================================================================

  bootstrap:
    desc: Apply ArgoCD bootstrap manifests (if inlineManifests failed)
    cmds:
      - kubectl apply -k apps/bootstrap/overlays/prod

  render-manifests:
    desc: Render/update bootstrap manifests (Cilium, ArgoCD)
    cmds:
      - ./scripts/render-bootstrap-manifests.sh

  secrets:
    desc: Initialize OpenBao secrets
    cmds:
      - ./scripts/init_secrets.sh

  kubeconfig:
    desc: Retrieve kubeconfig from OpenTofu outputs
    dir: infrastructure/opentofu/cluster
    cmds:
      - tofu output -raw kubeconfig > kubeconfig
      - tofu output -raw talosconfig > talosconfig
      - echo "KUBECONFIG and TALOSCONFIG written to infrastructure/opentofu/cluster/"
