extends: default

# Vendor/generated files — downloaded manifests we don't control.
ignore: |
  **/external-secrets/install.yaml
  **/traefik/standard-install.yaml
  **/linkerd/linkerd-crds.yaml
  **/linkerd/linkerd-control-plane.yaml
  **/bootstrap-manifests/flux-install.yaml
  **/bootstrap-manifests/cilium.yaml
  **/bootstrap-manifests/cilium-local.yaml

rules:
  document-start: disable
  # `on:` is the standard GitHub Actions trigger key — YAML 1.1 reads it as a
  # boolean. check-keys: false stops that false positive on every workflow
  # file while still catching a real unquoted yes/no/on/off used as a value.
  truthy:
    check-keys: false
  line-length:
    max: 120
    allow-non-breakable-words: true
  comments-indentation: disable
  # Kubernetes manifests use both indented and non-indented block sequences.
  # 'whatever' accepts both styles to avoid rewriting upstream manifest examples.
  indentation:
    spaces: 2
    indent-sequences: whatever
    check-multi-line-strings: false
